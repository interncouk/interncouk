files:
  "/etc/nginx/conf.d/custom.conf":
    mode: "000644"
    owner: root
    group: root
    content: |
     server {
        listen 80;
        root /var/www/html/public/;
        index index.php;
        location / {
            try_files $uri $uri/ /index.php?$query_string;
        }
    }